{
	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	"forwardPorts": [3000],
	// Comment out to connect as root instead.
	"remoteUser": "vscode",
	"containerEnv": {
		"TEAMS_CLOUD_DEPLOYMENT_ENV": "jun1"
	},
	"postCreateCommand": "bash .devcontainer/setup.sh"
}
